# 后端改造 TODO

## 保存校验

- [ ] 保存前校验规则对象结构完整性
- [ ] 保存前校验条件、提示、关联关系可序列化
- [ ] 保存前校验保存结果可完整回显
- [ ] 保存前校验引用资源存在且状态可用
- [ ] 允许保存草稿，但禁止写入非法结构

## 保存接口返回

- [ ] 保存接口返回字段级错误清单
- [ ] 保存接口返回区域级错误清单
- [ ] 保存接口返回对象级错误清单
- [ ] 统一错误码与错误信息结构

## 发布校验

- [ ] 发布前检查同页同字段跨规则、跨场景冲突
- [ ] 发布前检查覆盖策略确认是否完成
- [ ] 发布前检查日志留痕配置是否完整
- [ ] 发布前检查责任人是否完整
- [ ] 发布前检查适用范围是否完整
- [ ] 发布前检查生效时间和失效时间是否完整
- [ ] 发布前复检引用资源是否仍然有效
- [ ] 发布前检查执行方式特殊项配置是否完整（预览确认、补充按钮配置）
- [ ] 发布前检查人工作业时长等发布控制信息
- [ ] 发布前检查待发布版本集合是否形成一致版本视图

## 发布流程

- [ ] 将发布动作接入自动最终校验
- [ ] 保留最终门禁，未通过不得生效
- [ ] 保留发布检查结果与独立留痕
- [ ] 保留冲突确认动作与独立留痕
- [ ] 确保状态流转仍按待发布 -> 生效中执行
- [ ] 增加规则到期自动转 `EXPIRED` 的后台定时处理与审计留痕

## 名单检索

- [x] 明确名单基础信息落库，名单明细数据写入 ES
- [ ] 设计名单元数据表 / 名单版本表，承载名称、机构、有效期、导入文件、解析字段、输出字段等基础信息
- [ ] 设计 ES 索引结构，支持按解析字段建立精确检索能力，并承载输出字段原始值
- [ ] 提供名单导入解析接口，返回解析字段、字段属性、数据条数、错误明细
- [ ] 保存接口校验输出字段必须来自解析字段，禁止写入非法字段映射
- [ ] 运行时检索接口支持规则 / 作业按需传入匹配字段，不在名单资产侧固化主检索列
- [ ] 发布前校验名单索引已构建完成且当前输出字段不会破坏既有规则 / 作业引用
- [ ] 变更名单字段时补兼容性检查，识别被删除字段、被移出输出范围字段、类型变化字段
- [ ] 明确名单版本切换与 ES 别名切换策略，避免运行时读到半成品数据

## 审计与统计

- [ ] 审计日志区分配置错误、保存阻断、发布阻断
- [ ] 记录冲突检查结果与覆盖策略确认结果
- [ ] 增加问题前移效果统计口径

## 联调检查

- [ ] 校验保存接口返回结构支持前端精确定位
- [ ] 校验发布接口自动触发最终校验
- [ ] 校验权限边界未被配置流程绕过
