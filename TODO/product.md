# 产品改造 TODO

## 校验分层口径

- [ ] 明确前端实时校验范围
- [ ] 明确保存校验范围
- [ ] 明确发布校验范围
- [ ] 统一三层校验规则表

## 交互流程

- [ ] 将用户流程定义为“编辑 -> 保存草稿/提交发布 -> 自动发布校验 -> 生效”
- [ ] 取消“必须先手工点校验再发布”的强制路径
- [ ] 保留发布失败后的结构化阻断反馈
- [ ] 保持流程上下文信息持续可见

## 职责与门禁边界

- [ ] 保持配置角色与发布角色职责分离
- [ ] 明确自动执行无需独立风险确认动作，保留发布检查与审计承接
- [ ] 保持冲突确认与审计留痕独立存在
- [ ] 保持所有正式生效对象都经过发布门禁

## 发布与菜单能力

- [x] 明确业务发布对象仅包括 `API`、`名单`、`智能提示`、`作业`
- [x] 明确“配置发布”和“菜单能力配置”是两条不同处理链路，不混成一次发布动作
- [x] 明确所有菜单默认已预注入 `JSSDK`，菜单侧不再配置“是否启用 JSSDK”
- [x] 明确菜单侧只配置是否启用 `智能提示` / `智能作业`、菜单槽位、机构范围和 IP 试点范围
- [x] 明确普通业务配置人员无权配置菜单能力；该能力默认仅业务超管或被单独授权的菜单开通管理员可操作
- [ ] 冻结 `配置发布` 与 `菜单能力配置` 在产品导航、页面结构和权限可见性上的最终方案
- [ ] 冻结机构范围与 IP 试点的运行时命中规则与文案表达

## 名单检索

- [x] 明确名单基础信息保存数据库，名单明细数据保存 ES
- [x] 明确名单检索是规则 / 作业运行时的数据来源，不并入单纯智能提示来源枚举
- [x] 明确运行时匹配字段在规则 / 作业配置时选择，不再设计“主检索列”
- [x] 明确导入字段来自文件解析结果，只做只读预览，不由业务手工维护
- [x] 明确输出字段由上传名单的业务人员从解析表头中选择，供下游下拉引用
- [ ] 冻结真实上传解析接口的返回口径，包括解析字段、字段属性、数据条数和异常信息
- [ ] 明确名单变更对既有规则 / 作业输出字段兼容性的发布门禁

## 页面与文案

- [ ] 统一实时校验、保存阻断、发布阻断三类提示文案
- [ ] 统一“保存草稿”“立即生效”“生效失败”等关键文案
- [ ] 设计发布结果页展示结构

## 验收口径

- [ ] 编辑态可发现大部分低级错误
- [ ] 保存阶段能拦截非法结构
- [ ] 发布阶段重点拦截发布控制和一致性问题
- [ ] 用户无需额外进入独立校验流程
- [ ] 所有正式生效对象都经过最终发布门禁

## 推进顺序

- [ ] 先冻结三层校验口径
- [ ] 再推进前后端联动改造
- [ ] 最后补齐审计与指标统计
